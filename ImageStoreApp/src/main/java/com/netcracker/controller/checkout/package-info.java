package com.imagestore.controller.checkout;

/**
 * Provides the controllers necessary to process GET- and POST- checkout
 * requests from the MVC view. CheckoutGetController forms template to display
 * it, CheckoutPostController fills in checkout information and sends the email
 * to the user.
 *
 */
