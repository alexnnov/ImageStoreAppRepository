package com.imagestore.controller.shipping;

/**
 * Provides the controllers necessary to process requests connected to operations with shipping addresses:
 * adding new shipping address,
 * displaying user's available shipping addresses,
 * deleting shipping address,
 * setting a shipping address as default,
 * updating shipping address info.
 */